node_modules
dist
dist-ssr
*.local
.DS_Store
.vite

# TypeScript incremental build cache — regenerated by every `npm run build`
tsconfig.tsbuildinfo

# Astro's generated content types — regenerated on dev and build
.astro/
