node_modules
dist
dist-ssr
*.local
.DS_Store
.vite

# Secrets. .env.example is committed as the template; the real .env is not.
.env
.env.*
!.env.example
.vercel

# TypeScript incremental build cache — regenerated by every `npm run build`
tsconfig.tsbuildinfo

# Astro's generated content types — regenerated on dev and build
.astro/
