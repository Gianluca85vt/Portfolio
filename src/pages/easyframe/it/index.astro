---
// Generata da EasyFrame/landing/sito.py: modifica la sorgente la', non questo file.
---
<!doctype html>
<html lang="it">
<head>
<script is:inline>
  (function () {
    try {
      if (localStorage.getItem('gs-no-track') === '1') {
        window['ga-disable-G-D0BYF0KT6L'] = true;
        window.__gsNoTrack = true;
      }
    } catch (e) {}
  })();
</script>
<script is:inline>
  if (!window.__gsNoTrack) {
    var s = document.createElement('script');
    s.async = true;
    s.src = 'https://www.googletagmanager.com/gtag/js?id=G-D0BYF0KT6L';
    document.head.appendChild(s);
    window.dataLayer = window.dataLayer || [];
    window.gtag = function () { dataLayer.push(arguments); };
    gtag('js', new Date());
    gtag('config', 'G-D0BYF0KT6L');
  }
</script>
<meta charset="utf-8">
<meta name="viewport" content="width=device-width, initial-scale=1">
<title>EasyFrame — immagini e video con l&#x27;AI, con il costo scritto prima</title>
<meta name="description" content="Immagini e video con l&#x27;AI, sulla tua GPU o nel cloud. EasyFrame ti dice quanto costa, se ci sta nella tua scheda e cosa manca, prima che tu clicchi. Windows, 29 €.">
<meta name="theme-color" content="#0B0B12">
<link rel="icon" type="image/png" href="/favicon.png">
<link rel="canonical" href="https://www.gianlucascattarella.it/easyframe/it/">
<link rel="alternate" hreflang="en" href="https://www.gianlucascattarella.it/easyframe/">
<link rel="alternate" hreflang="it" href="https://www.gianlucascattarella.it/easyframe/it/">
<link rel="alternate" hreflang="x-default" href="https://www.gianlucascattarella.it/easyframe/">
<meta property="og:type" content="product">
<meta property="og:site_name" content="Gianluca Scattarella">
<meta property="og:locale" content="it_IT">
<meta property="og:url" content="https://www.gianlucascattarella.it/easyframe/it/">
<meta property="og:title" content="EasyFrame — immagini e video con l&#x27;AI, con il costo scritto prima">
<meta property="og:description" content="Immagini e video con l&#x27;AI, sulla tua GPU o nel cloud. EasyFrame ti dice quanto costa, se ci sta nella tua scheda e cosa manca, prima che tu clicchi. Windows, 29 €.">
<meta property="og:image" content="https://www.gianlucascattarella.it/easyframe/app-clean.png">
<meta name="twitter:card" content="summary_large_image">
<meta name="twitter:title" content="EasyFrame — immagini e video con l&#x27;AI, con il costo scritto prima">
<meta name="twitter:description" content="Immagini e video con l&#x27;AI, sulla tua GPU o nel cloud. EasyFrame ti dice quanto costa, se ci sta nella tua scheda e cosa manca, prima che tu clicchi. Windows, 29 €.">
<meta name="twitter:image" content="https://www.gianlucascattarella.it/easyframe/app-clean.png">
<link rel="preconnect" href="https://fonts.googleapis.com">
<link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
<link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700;800;900&family=JetBrains+Mono:wght@400;500;700&display=swap" rel="stylesheet">
<style is:global>
:root{
  --bg:#0B0B12; --bg2:#101019; --surface:#15151F; --surface2:#1C1C29;
  --border:#2A2A3C; --border-soft:#1F1F2E;
  --text:#F1F1F6; --dim:#9B9BB2; --faint:#666680;
  --accent:#7C5CFF; --accent-hi:#9B84FF; --cyan:#22D3EE;
  --ok:#34D399; --warn:#FBBF24; --danger:#F87171;
  --grad:linear-gradient(135deg,#7C5CFF 0%,#8B6BFF 40%,#22D3EE 100%);
  --wrap:1180px;
  --mono:"JetBrains Mono",ui-monospace,"Cascadia Code",Consolas,monospace;
}
*{box-sizing:border-box}
html{scroll-behavior:smooth}
body{
  margin:0; background:var(--bg); color:var(--text);
  font-family:Inter,"Segoe UI Variable Text","Segoe UI",system-ui,-apple-system,sans-serif;
  font-size:16px; line-height:1.6; -webkit-font-smoothing:antialiased;
  overflow-x:hidden;
}
img{max-width:100%; display:block}
a{color:inherit; text-decoration:none}
h1,h2,h3{margin:0; letter-spacing:-.025em; line-height:1.08; font-weight:800}
p{margin:0}
.wrap{max-width:var(--wrap); margin:0 auto; padding:0 24px; position:relative; z-index:2}

/* ---------------------------------------------------------------- ritmo */
section{padding:clamp(76px,9vw,142px) 0; position:relative}
.rule{height:1px;background:linear-gradient(90deg,transparent,var(--border-soft) 18%,var(--border-soft) 82%,transparent)}
.eyebrow{
  font-size:11.5px; font-weight:700; letter-spacing:.18em; text-transform:uppercase;
  color:var(--accent-hi); margin-bottom:18px; display:flex; align-items:center; gap:10px;
}
.eyebrow::before{content:""; width:22px; height:1px; background:var(--accent); opacity:.7}
h2{font-size:clamp(30px,4.3vw,52px)}
h3{font-size:19px; font-weight:700; letter-spacing:-.01em}
.lede{color:var(--dim); font-size:clamp(16px,1.5vw,18.5px); max-width:660px; margin-top:20px}
.mono{font-family:var(--mono); font-variant-numeric:tabular-nums}

/* --------------------------------------------------------------- bottoni */
.btn{
  display:inline-flex; align-items:center; justify-content:center; gap:10px;
  height:52px; padding:0 26px; border-radius:999px; font-weight:700; font-size:15px;
  border:1px solid transparent; cursor:pointer; white-space:nowrap;
  transition:transform .18s cubic-bezier(.2,.7,.3,1), box-shadow .25s, background .25s, border-color .25s;
}
.btn-primary{
  background:var(--grad); color:#0A0A10;
  box-shadow:0 10px 34px -12px rgba(124,92,255,.85), inset 0 1px 0 rgba(255,255,255,.35);
}
.btn-primary:hover{transform:translateY(-2px); box-shadow:0 18px 48px -14px rgba(124,92,255,1)}
.btn-ghost{background:rgba(255,255,255,.03); border-color:var(--border); color:var(--text)}
.btn-ghost:hover{background:rgba(255,255,255,.07); border-color:#3E3E5C; transform:translateY(-2px)}
.btn-sm{height:42px; padding:0 20px; font-size:14px}
.strike{text-decoration:line-through; opacity:.5; font-weight:600}

/* ------------------------------------------------------------------- nav */
.nav{
  position:fixed; top:14px; left:50%; transform:translateX(-50%); z-index:60;
  display:flex; align-items:center; gap:8px;
  padding:8px 8px 8px 16px; border-radius:999px;
  background:rgba(16,16,25,.72); border:1px solid var(--border-soft);
  backdrop-filter:blur(16px) saturate(140%); -webkit-backdrop-filter:blur(16px) saturate(140%);
  box-shadow:0 12px 40px -18px rgba(0,0,0,.9);
  transition:border-color .3s, background .3s;
  max-width:calc(100vw - 28px);
}
.nav.scrolled{border-color:var(--border); background:rgba(16,16,25,.92)}
.brand{display:flex; align-items:center; gap:9px; font-weight:800; letter-spacing:-.02em; padding-right:6px}
.mark{
  width:26px; height:26px; border-radius:8px; background:var(--grad);
  box-shadow:0 0 0 1px rgba(255,255,255,.14), 0 6px 18px -8px rgba(124,92,255,.9);
  position:relative; flex:none;
}
.mark::after{content:""; position:absolute; inset:7px; border:1.6px solid rgba(10,10,16,.85); border-radius:3px}
.nav-links{display:flex; gap:2px; margin:0 4px}
.nav-links a{
  padding:9px 13px; border-radius:999px; font-size:13.5px; color:var(--dim); font-weight:500;
  transition:color .2s, background .2s;
}
.nav-links a:hover{color:var(--text); background:rgba(255,255,255,.05)}
.lang{
  padding:8px 11px; border-radius:999px; font-size:12px; font-weight:700; letter-spacing:.08em;
  color:var(--dim); border:1px solid var(--border-soft); transition:color .2s, border-color .2s;
}
.lang:hover{color:var(--text); border-color:var(--border)}

/* ------------------------------------------------------------------ hero */
.hero{padding-top:clamp(120px,15vw,190px); padding-bottom:0; overflow:hidden}
.glow{position:absolute; inset:0; overflow:hidden; pointer-events:none; z-index:0}
.glow::before{
  content:""; position:absolute; top:-340px; left:50%; transform:translateX(-50%);
  width:1200px; height:760px; border-radius:50%;
  background:radial-gradient(ellipse at center, rgba(124,92,255,.34), rgba(124,92,255,.10) 42%, transparent 68%);
  filter:blur(28px);
}
.glow::after{
  content:""; position:absolute; top:60px; right:-160px; width:620px; height:520px; border-radius:50%;
  background:radial-gradient(ellipse at center, rgba(34,211,238,.16), transparent 66%);
  filter:blur(34px);
}
.grid-bg{
  position:absolute; inset:0; z-index:0; pointer-events:none; opacity:.5;
  background-image:linear-gradient(rgba(255,255,255,.035) 1px,transparent 1px),
                   linear-gradient(90deg,rgba(255,255,255,.035) 1px,transparent 1px);
  background-size:72px 72px;
  -webkit-mask-image:radial-gradient(ellipse 78% 58% at 50% 12%,#000 20%,transparent 76%);
          mask-image:radial-gradient(ellipse 78% 58% at 50% 12%,#000 20%,transparent 76%);
}
.badge{
  display:inline-flex; align-items:center; gap:9px; padding:7px 15px 7px 11px; border-radius:999px;
  border:1px solid var(--border); background:rgba(28,28,41,.7);
  font-size:12.8px; color:var(--dim); font-weight:500;
}
.dot{width:6px; height:6px; border-radius:50%; background:var(--ok); box-shadow:0 0 0 4px rgba(52,211,153,.16); flex:none}
.hero h1{font-size:clamp(34px,5.4vw,64px); font-weight:800; letter-spacing:-.035em; margin:26px 0 0; line-height:1.12}
.hero h1 .g{
  background:linear-gradient(100deg,#B9A6FF 0%,#7C5CFF 42%,#22D3EE 100%);
  -webkit-background-clip:text; background-clip:text; color:transparent;
}
/* La parola che porta tutto il peso del messaggio: sta su una riga sua. */
.hero h1 .prima{
  display:block; font-size:1.75em; font-weight:900; letter-spacing:-.02em;
  line-height:.98; margin:.06em 0 .02em;
}
.hero .lede{font-size:clamp(16.5px,1.8vw,20px); margin-top:26px}
.cta-row{display:flex; flex-wrap:wrap; gap:14px; margin-top:38px; align-items:center}
.microcopy{margin-top:18px; color:var(--faint); font-size:13.5px}
.microcopy b{color:var(--dim); font-weight:600}

.shot{position:relative; margin-top:clamp(52px,7vw,86px)}
.shot::before{
  content:""; position:absolute; left:50%; top:14%; transform:translateX(-50%);
  width:82%; height:74%; border-radius:50%; z-index:0;
  background:radial-gradient(ellipse at center, rgba(124,92,255,.5), rgba(34,211,238,.18) 46%, transparent 72%);
  filter:blur(72px);
}
.shot .frame{position:relative; z-index:1; border-radius:14px}
.shot img{
  border-radius:14px; width:100%;
  box-shadow:0 70px 130px -50px rgba(0,0,0,.95), 0 0 0 1px rgba(255,255,255,.07);
}
.shot-cap{position:relative; z-index:2; margin-top:20px; text-align:center; color:var(--faint); font-size:12.8px}
.only-mobile{display:none}

/* ------------------------------------------------------------ striscia n */
.stats{
  display:grid; grid-template-columns:repeat(4,1fr); gap:1px;
  background:var(--border-soft); border:1px solid var(--border-soft); border-radius:18px; overflow:hidden;
}
.stat{background:linear-gradient(180deg,#101019,#0D0D15); padding:30px 24px; text-align:center}
.stat .n{font-size:clamp(24px,3.2vw,38px); font-weight:800; letter-spacing:-.03em; font-family:var(--mono)}
.stat .n.g{background:var(--grad); -webkit-background-clip:text; background-clip:text; color:transparent}
.stat .l{color:var(--faint); font-size:12.5px; margin-top:8px; line-height:1.4}

/* ----------------------------------------------------------------- card */
.card{
  position:relative; overflow:hidden;
  background:linear-gradient(180deg,#15151F 0%,#101019 100%);
  border:1px solid var(--border-soft); border-radius:20px; padding:28px;
  transition:transform .3s cubic-bezier(.2,.7,.3,1), border-color .3s, box-shadow .3s;
}
.card::before{
  content:""; position:absolute; inset:0; opacity:0; transition:opacity .35s; pointer-events:none;
  background:radial-gradient(420px circle at var(--mx,50%) var(--my,0%), rgba(124,92,255,.14), transparent 62%);
}
.card:hover{transform:translateY(-5px); border-color:#37375A; box-shadow:0 26px 60px -28px rgba(124,92,255,.55)}
.card:hover::before{opacity:1}
.card h3{margin-bottom:10px}
.card p{color:var(--dim); font-size:14.6px}
.card .ic{
  width:40px; height:40px; border-radius:12px; display:grid; place-items:center; margin-bottom:18px;
  background:rgba(124,92,255,.12); border:1px solid rgba(124,92,255,.3); color:var(--accent-hi);
}
.card .ic svg{width:19px; height:19px; stroke:currentColor; fill:none; stroke-width:1.7; stroke-linecap:round; stroke-linejoin:round}
.grid-3{display:grid; grid-template-columns:repeat(3,1fr); gap:20px; margin-top:52px}
.grid-2{display:grid; grid-template-columns:repeat(2,1fr); gap:20px; margin-top:52px}

/* -------------------------------------------------------------- domande */
.qa{display:grid; grid-template-columns:repeat(2,1fr); gap:18px; margin-top:52px}
.qa-item{
  border:1px solid var(--border-soft); border-radius:18px; padding:26px 26px 24px;
  background:linear-gradient(180deg,rgba(21,21,31,.9),rgba(13,13,21,.9));
  transition:border-color .3s, background .3s;
}
.qa-item:hover{border-color:#37375A}
.qa-q{color:var(--faint); font-size:14px; font-style:italic}
.qa-a{margin-top:14px; font-size:17px; font-weight:600; letter-spacing:-.01em}
.chip{
  display:inline-flex; align-items:center; gap:8px; margin-top:16px;
  padding:7px 13px; border-radius:9px; font-family:var(--mono); font-size:12.6px;
  background:#0C0C14; border:1px solid var(--border); color:var(--dim);
}
.chip .sq{width:7px; height:7px; border-radius:2px; background:var(--accent); flex:none}
.chip.ok .sq{background:var(--ok)} .chip.warn .sq{background:var(--warn)}

/* -------------------------------------------------------------- tabella */
.tablewrap{overflow-x:auto; margin-top:44px; border:1px solid var(--border-soft); border-radius:18px; background:#0E0E16}
table{border-collapse:collapse; width:100%; min-width:620px; font-size:14.6px}
th,td{padding:16px 20px; text-align:left; border-bottom:1px solid var(--border-soft)}
thead th{
  color:var(--faint); font-size:11.5px; font-weight:700; letter-spacing:.12em; text-transform:uppercase;
  background:#0B0B12;
}
tbody tr{transition:background .2s}
tbody tr:hover{background:rgba(124,92,255,.05)}
tbody tr:last-child td{border-bottom:none}
td.num{font-family:var(--mono); font-variant-numeric:tabular-nums; white-space:nowrap}
.free{color:var(--ok); font-weight:700}
.tag{
  display:inline-block; padding:3px 9px; border-radius:6px; font-size:11.5px; font-weight:700;
  letter-spacing:.04em; text-transform:uppercase; white-space:nowrap;
}
.tag.serie{background:rgba(124,92,255,.16); color:var(--accent-hi); border:1px solid rgba(124,92,255,.3)}
.tag.opt{background:rgba(255,255,255,.05); color:var(--faint); border:1px solid var(--border)}

/* --------------------------------------------------------------- preset */
.presets{display:grid; grid-template-columns:repeat(3,1fr); gap:14px; margin-top:52px}
.preset{
  position:relative; overflow:hidden; padding:22px;
  border:1px solid var(--border-soft); border-radius:16px; background:#101019;
  transition:transform .28s cubic-bezier(.2,.7,.3,1), border-color .28s, background .28s;
}
.preset::after{
  content:""; position:absolute; left:0; top:0; bottom:0; width:3px; background:var(--grad);
  transform:scaleY(0); transform-origin:top; transition:transform .3s;
}
.preset:hover{transform:translateY(-4px); border-color:#37375A; background:#13131E}
.preset:hover::after{transform:scaleY(1)}
.preset .pn{font-weight:700; font-size:15.5px; letter-spacing:-.01em}
.preset .pm{color:var(--faint); font-size:12.4px; margin-top:7px}
.preset .pp{
  margin-top:14px; font-family:var(--mono); font-size:11.6px; color:var(--accent-hi);
  background:rgba(124,92,255,.09); border:1px solid rgba(124,92,255,.18);
  border-radius:7px; padding:6px 9px; display:inline-block;
}

/* ------------------------------------------------------------- due col */
.split{display:grid; grid-template-columns:1fr 1fr; gap:clamp(32px,5vw,72px); align-items:center; margin-top:52px}
.figure{position:relative}
.figure::before{
  content:""; position:absolute; inset:-10% -6%; z-index:0; border-radius:50%;
  background:radial-gradient(ellipse at center, rgba(124,92,255,.34), transparent 68%); filter:blur(56px);
}
.figure img{
  position:relative; z-index:1; border-radius:16px;
  box-shadow:0 40px 90px -40px rgba(0,0,0,.95), 0 0 0 1px rgba(255,255,255,.07);
}
.rules{list-style:none; padding:0; margin:0; counter-reset:r}
.rules li{
  counter-increment:r; position:relative; padding:16px 0 16px 54px;
  border-bottom:1px solid var(--border-soft); color:var(--dim); font-size:15px;
}
.rules li:last-child{border-bottom:none}
.rules li::before{
  content:counter(r,decimal-leading-zero); position:absolute; left:0; top:15px;
  font-family:var(--mono); font-size:12px; font-weight:700; color:var(--accent-hi);
  width:32px; height:26px; display:grid; place-items:center;
  border:1px solid rgba(124,92,255,.3); border-radius:7px; background:rgba(124,92,255,.09);
}
.rules b{color:var(--text); font-weight:600}

/* ------------------------------------------------------------- citazione */
.quote{
  margin-top:52px; padding:clamp(32px,5vw,58px); border-radius:24px; text-align:center;
  background:linear-gradient(180deg,rgba(124,92,255,.13),rgba(34,211,238,.05));
  border:1px solid rgba(124,92,255,.24); position:relative; overflow:hidden;
}
.quote .big{
  font-family:var(--mono); font-size:clamp(17px,3vw,30px); font-weight:500; letter-spacing:-.02em;
  color:#fff; line-height:1.35;
}
.quote .big em{color:var(--warn); font-style:normal}
.quote .sub{color:var(--dim); margin-top:20px; font-size:15px; max-width:620px; margin-left:auto; margin-right:auto}

/* -------------------------------------------------------------- galleria */
.gallery{display:grid; grid-template-columns:repeat(3,1fr); gap:16px; margin-top:52px}
.tile{
  position:relative; border-radius:16px; overflow:hidden; border:1px solid var(--border-soft);
  aspect-ratio:4/5; display:flex; flex-direction:column; justify-content:flex-end;
  padding:20px; transition:transform .3s cubic-bezier(.2,.7,.3,1), border-color .3s, box-shadow .3s;
}
.tile:hover{transform:translateY(-5px); border-color:#3B3B60; box-shadow:0 30px 60px -30px rgba(0,0,0,.9)}
.tile .art{position:absolute; inset:0; z-index:0}
.tile .art img{position:absolute; inset:0; width:100%; height:100%; object-fit:cover}
.tile .art::after{
  content:""; position:absolute; inset:0;
  background:linear-gradient(180deg,rgba(11,11,18,0) 30%,rgba(11,11,18,.86) 82%,rgba(11,11,18,.96));
}
.tile .meta{position:relative; z-index:1}
.tile .pname{font-weight:700; font-size:14.5px}
.tile .ptext{color:var(--dim); font-size:12.6px; margin-top:5px; line-height:1.45}
.tile .pset{margin-top:11px; font-family:var(--mono); font-size:11px; color:var(--faint)}
.tile .slot{
  position:absolute; top:14px; left:14px; z-index:1; padding:5px 10px; border-radius:7px;
  font-size:10.5px; font-weight:600; letter-spacing:.06em; text-transform:uppercase;
  background:rgba(11,11,18,.6); border:1px solid rgba(255,255,255,.12); color:var(--dim);
  backdrop-filter:blur(6px);
}
.a1{background:radial-gradient(120% 80% at 20% 10%,#3B2E6B,#141422 60%,#0D0D15)}
.a2{background:radial-gradient(120% 80% at 80% 15%,#123C4A,#111A26 58%,#0D0D15)}
.a3{background:radial-gradient(120% 80% at 40% 0%,#4A2547,#1B1524 60%,#0D0D15)}
.a4{background:radial-gradient(120% 80% at 70% 20%,#2A3A6B,#131826 58%,#0D0D15)}
.a5{background:radial-gradient(120% 80% at 25% 15%,#5A3A22,#221A18 60%,#0D0D15)}
.a6{background:radial-gradient(120% 80% at 60% 5%,#1F4740,#12201E 58%,#0D0D15)}

/* --------------------------------------------------------------- codice */
.code{
  margin-top:26px; border:1px solid var(--border-soft); border-radius:14px; background:#0A0A11;
  overflow-x:auto; padding:20px 22px; font-family:var(--mono); font-size:12.6px;
  color:var(--dim); line-height:1.7; white-space:pre;
}
.code .k{color:var(--accent-hi)}
.code .s{color:#6FE0BC}
.code .c{color:var(--faint)}
/* <pre> e non <div>: Astro comprime gli spazi fuori da pre, e il blocco
   finirebbe su una riga sola. */
pre.code{margin:26px 0 0}
.guards{display:grid; grid-template-columns:repeat(3,1fr); gap:16px; margin-top:26px}
.guard{
  border:1px solid var(--border-soft); border-radius:16px; padding:22px; background:#101019;
  transition:border-color .3s, transform .28s cubic-bezier(.2,.7,.3,1);
}
.guard:hover{border-color:#37375A; transform:translateY(-4px)}
.guard b{display:block; font-size:15px; margin-bottom:8px}
.guard p{color:var(--dim); font-size:14px}
.guard .no{
  display:inline-block; font-family:var(--mono); font-size:11px; letter-spacing:.06em;
  text-transform:uppercase; color:var(--danger); border:1px solid rgba(248,113,113,.32);
  background:rgba(248,113,113,.08); border-radius:6px; padding:3px 8px; margin-bottom:12px;
}

/* -------------------------------------------------------------- limiti */
.limits-box{margin-top:40px}
.limits-toggle{
  list-style:none; cursor:pointer; display:inline-flex; align-items:center; gap:12px;
  height:52px; padding:0 26px; border-radius:999px; font-weight:700; font-size:15px;
  border:1px solid rgba(251,191,36,.35); background:rgba(251,191,36,.07); color:var(--warn);
  transition:background .25s, border-color .25s, transform .18s cubic-bezier(.2,.7,.3,1);
}
.limits-toggle::-webkit-details-marker{display:none}
.limits-toggle:hover{background:rgba(251,191,36,.14); border-color:rgba(251,191,36,.6); transform:translateY(-2px)}
.limits-toggle::after{
  content:""; width:8px; height:8px; margin-left:2px;
  border-right:2px solid currentColor; border-bottom:2px solid currentColor;
  transform:translateY(-2px) rotate(45deg); transition:transform .3s;
}
.limits-box[open] .limits-toggle::after{transform:translateY(1px) rotate(-135deg)}
.limits-toggle .chiudi{display:none}
.limits-box[open] .limits-toggle .apri{display:none}
.limits-box[open] .limits-toggle .chiudi{display:inline}
.limits{display:grid; grid-template-columns:repeat(2,1fr); gap:16px; margin-top:26px}
/* La cascata parte solo se c'e' il JS: senza, i limiti si vedono e basta. */
.js .limits .limit{opacity:0; transform:translateY(14px); transition:opacity .5s cubic-bezier(.2,.7,.3,1), transform .5s cubic-bezier(.2,.7,.3,1)}
.js .limits-box.ready .limit{opacity:1; transform:none}
.limits .limit:nth-child(1){transition-delay:.00s}
.limits .limit:nth-child(2){transition-delay:.06s}
.limits .limit:nth-child(3){transition-delay:.12s}
.limits .limit:nth-child(4){transition-delay:.18s}
.limits .limit:nth-child(5){transition-delay:.24s}
.limits .limit:nth-child(6){transition-delay:.30s}
.limit{
  border-left:2px solid rgba(251,191,36,.5); border-radius:0 14px 14px 0;
  background:linear-gradient(90deg,rgba(251,191,36,.06),rgba(251,191,36,0) 60%);
  padding:20px 22px; transition:background .3s, border-color .3s;
}
.limit:hover{border-left-color:var(--warn); background:linear-gradient(90deg,rgba(251,191,36,.1),rgba(251,191,36,0) 65%)}
.limit b{display:block; font-size:15px; font-weight:700; margin-bottom:7px}
.limit p{color:var(--dim); font-size:14.2px}

/* --------------------------------------------------------------- prezzo */
.price-wrap{max-width:560px; margin:56px auto 0; position:relative}
.price-wrap::before{
  content:""; position:absolute; inset:-2px; border-radius:26px; background:var(--grad);
  opacity:.5; filter:blur(16px); z-index:0;
}
.price{
  position:relative; z-index:1; border-radius:24px; padding:clamp(30px,4vw,44px);
  background:linear-gradient(180deg,#16161F,#0D0D15); border:1px solid rgba(124,92,255,.35);
  text-align:center;
}
.price .kicker{font-size:11.5px; font-weight:700; letter-spacing:.18em; text-transform:uppercase; color:var(--faint)}
.price .amount{display:flex; align-items:baseline; justify-content:center; gap:14px; margin-top:20px}
.price .now{font-size:clamp(52px,8vw,76px); font-weight:900; letter-spacing:-.045em; line-height:1; font-family:var(--mono)}
.price .was{font-size:26px; font-family:var(--mono)}
.price .off{
  display:inline-block; margin-top:14px; padding:5px 12px; border-radius:999px; font-size:12px; font-weight:700;
  background:rgba(52,211,153,.14); color:var(--ok); border:1px solid rgba(52,211,153,.3);
}
.price .once{color:var(--dim); margin-top:16px; font-size:14.5px}
.plist{list-style:none; padding:0; margin:30px 0; text-align:left}
.plist li{display:flex; gap:12px; padding:10px 0; color:var(--dim); font-size:14.8px; align-items:flex-start}
.plist svg{width:17px; height:17px; flex:none; margin-top:3px; stroke:var(--ok); fill:none; stroke-width:2.4; stroke-linecap:round; stroke-linejoin:round}
.price .btn{width:100%}
.price .safe{margin-top:16px; color:var(--faint); font-size:12.6px}
.todo{
  display:inline-block; padding:2px 8px; border-radius:6px; font-family:var(--mono); font-size:12px;
  background:rgba(248,113,113,.14); color:#FCA5A5; border:1px dashed rgba(248,113,113,.5);
}

/* ------------------------------------------------------------------ faq */
.faq{margin-top:52px; border-top:1px solid var(--border-soft)}
.faq details{border-bottom:1px solid var(--border-soft)}
.faq summary{
  list-style:none; cursor:pointer; padding:24px 44px 24px 0; position:relative;
  font-size:17px; font-weight:600; letter-spacing:-.01em; transition:color .2s;
}
.faq summary::-webkit-details-marker{display:none}
.faq summary:hover{color:var(--accent-hi)}
.faq summary::after{
  content:""; position:absolute; right:8px; top:30px; width:11px; height:11px;
  border-right:1.8px solid var(--faint); border-bottom:1.8px solid var(--faint);
  transform:rotate(45deg); transition:transform .3s, border-color .3s;
}
.faq details[open] summary::after{transform:rotate(-135deg); border-color:var(--accent-hi)}
.faq .ans{color:var(--dim); font-size:15px; padding:0 40px 26px 0; max-width:800px}
.faq .ans b{color:var(--text); font-weight:600}

/* ---------------------------------------------------------------- chiusa */
.final{
  position:relative; overflow:hidden; text-align:center;
  background:radial-gradient(ellipse 90% 70% at 50% 100%, rgba(124,92,255,.22), transparent 70%);
}
.final h2{font-size:clamp(32px,5.2vw,62px)}
footer{border-top:1px solid var(--border-soft); padding:44px 0 60px; color:var(--faint); font-size:13.5px}
.foot-row{display:flex; justify-content:space-between; gap:20px; flex-wrap:wrap; align-items:center}

/* ---------------------------------------------------------------- moto */
.reveal{opacity:0; transform:translateY(26px); transition:opacity .7s cubic-bezier(.2,.7,.3,1), transform .7s cubic-bezier(.2,.7,.3,1)}
.reveal.in{opacity:1; transform:none}

@media (max-width:960px){
  .grid-3,.presets,.gallery,.guards{grid-template-columns:repeat(2,1fr)}
  .split,.qa,.limits,.grid-2{grid-template-columns:1fr}
  .stats{grid-template-columns:repeat(2,1fr)}
  .nav-links{display:none}
}
@media (max-width:620px){
  .grid-3,.presets,.gallery,.guards{grid-template-columns:1fr}
  .cta-row .btn{width:100%}
  section{padding:66px 0}
  /* Su un telefono la finestra intera diventa una macchia: meglio a grandezza
     leggibile, con lo scorrimento orizzontale dentro la sua cornice. */
  .shot .frame{overflow-x:auto; -webkit-overflow-scrolling:touch}
  .shot .frame img{min-width:660px; box-shadow:0 0 0 1px rgba(255,255,255,.07)}
  .only-mobile{display:inline}
}
@media (prefers-reduced-motion:reduce){
  *{animation:none !important; transition:none !important}
  .reveal{opacity:1; transform:none}
  html{scroll-behavior:auto}
}
</style>
</head>
<body>
<nav class="nav" id="nav">
  <div class="brand"><span class="mark"></span>EasyFrame</div>
  <div class="nav-links">
    <a href="#come">Come funziona</a>
    <a href="#preset">Preset</a>
    <a href="#modelli">Modelli</a>
    <a href="#agenti">Agenti</a>
    <a href="#limiti">Limiti</a>
    <a href="#prezzo">Prezzo</a>
  </div>
  <a class="lang" href="/easyframe/" hreflang="en" lang="en">EN</a>
  <a class="btn btn-primary btn-sm" href="https://gianlucaiscool5.gumroad.com/l/easyframe">Comprala &middot; 29&nbsp;&euro;</a>
</nav>

<!-- ============================================================== HERO -->
<section class="hero">
  <div class="glow"></div>
  <div class="grid-bg"></div>
  <div class="wrap">
    <div style="text-align:center">
      <span class="badge"><span class="dot"></span> Versione 0.1.1 &middot; Windows 10 e 11 &middot; macOS in beta</span>
      <h1>Ti diciamo quanto costa<span class="g prima">PRIMA</span>che tu clicchi.</h1>
      <p class="lede" style="margin-left:auto;margin-right:auto">
        Immagini e video con l&rsquo;AI, sulla tua scheda video o nel cloud.
        EasyFrame ti mostra il prezzo in euro, la VRAM che serve e i modelli che
        mancano <b style="color:var(--text)">prima</b> di generare, non dopo.
      </p>
      <div class="cta-row" style="justify-content:center">
        <a class="btn btn-primary" href="https://gianlucaiscool5.gumroad.com/l/easyframe">
          Comprala &mdash; <span class="strike">45&nbsp;&euro;</span> 29&nbsp;&euro;
        </a>
        <a class="btn btn-ghost" href="#come">Guarda come funziona</a>
      </div>
      <p class="microcopy">
        Acquisto unico &middot; <b>nessun abbonamento</b> &middot; in locale il costo per immagine &egrave; zero
      </p>
    </div>

    <div class="shot reveal">
      <div class="frame">
        <img src="/easyframe/app-clean.png" alt="La finestra di EasyFrame: pannello dei controlli a sinistra, anteprima del risultato a destra">
      </div>
      <p class="shot-cap">La finestra reale dell&rsquo;applicazione. Nessun mockup.<span class="only-mobile"> Scorri per vederla tutta.</span></p>
    </div>
  </div>
</section>

<!-- ============================================================= NUMERI -->
<section style="padding-top:0">
  <div class="wrap">
    <div class="stats reveal">
      <div class="stat"><div class="n g">0 &euro;</div><div class="l">per immagine<br>in locale</div></div>
      <div class="stat"><div class="n">14</div><div class="l">preset pronti,<br>parametri gi&agrave; fissati</div></div>
      <div class="stat"><div class="n">10</div><div class="l">corredi di modelli<br>scaricabili dall&rsquo;app</div></div>
      <div class="stat"><div class="n">1&nbsp;520</div><div class="l">verifiche automatiche<br>su ogni versione</div></div>
    </div>
  </div>
</section>

<div class="rule"></div>

<!-- =========================================================== IL PUNTO -->
<section id="differenza">
  <div class="wrap">
    <div class="eyebrow">La differenza</div>
    <h2>Gli altri ti fanno cliccare<br>e sperare.</h2>
    <p class="lede">
      Quanto mi coster&agrave; questo video? Ci sta nella mia scheda? Perch&eacute; quel preset non
      parte? Che modello sta usando davvero? Sono quattro domande a cui di solito si
      risponde dopo, quando hai gi&agrave; speso tempo o soldi. EasyFrame risponde prima.
    </p>

    <div class="qa">
      <div class="qa-item reveal">
        <p class="qa-q">&ldquo;Quanto mi costa cliccare Genera?&rdquo;</p>
        <p class="qa-a">La stima in euro sta accanto al pulsante, sempre.</p>
        <span class="chip ok"><span class="sq"></span>Gratis &middot; GPU locale</span>
      </div>
      <div class="qa-item reveal">
        <p class="qa-q">&ldquo;Ci sta nella mia scheda video?&rdquo;</p>
        <p class="qa-a">Due numeri veri messi a confronto, non un bollino verde.</p>
        <span class="chip warn"><span class="sq"></span>~24 GB VRAM su 12 &middot; oltre la tua scheda</span>
      </div>
      <div class="qa-item reveal">
        <p class="qa-q">&ldquo;Perch&eacute; questo preset non parte?&rdquo;</p>
        <p class="qa-a">Ti dice quale modello manca, quanto pesa e con che licenza.</p>
        <span class="chip"><span class="sq"></span>Scarica &middot; 29.6 GB</span>
      </div>
      <div class="qa-item reveal">
        <p class="qa-q">&ldquo;Che parametri sta usando?&rdquo;</p>
        <p class="qa-a">Scritti in chiaro, presi dal template ufficiale del modello.</p>
        <span class="chip"><span class="sq"></span>8 step &middot; CFG 1 &middot; euler</span>
      </div>
    </div>
  </div>
</section>

<div class="rule"></div>

<!-- ========================================================== DUE STRADE -->
<section id="strade">
  <div class="wrap">
    <div class="eyebrow">Due strade, una finestra</div>
    <h2>Sulla tua GPU o nel cloud.<br>Si cambia con un click.</h2>
    <p class="lede">
      In locale non esce niente dal computer e non paghi nulla. Nel cloud usi la <b style="color:var(--text)">tua</b> chiave API: paghi il provider al suo prezzo, EasyFrame non
      ci mette sopra un centesimo.
    </p>

    <div class="tablewrap reveal">
      <table>
        <thead>
          <tr><th>Dove genera</th><th>Immagine</th><th>Video 8&nbsp;s</th><th>Prompt negativo e seed</th></tr>
        </thead>
        <tbody>
          <tr>
            <td><b>Locale</b> &mdash; la tua GPU</td>
            <td class="num free">gratis</td>
            <td class="num free">gratis</td>
            <td>s&igrave;</td>
          </tr>
          <tr>
            <td><b>OpenAI</b> &mdash; gpt-image-1 &middot; Sora 2</td>
            <td class="num">&asymp; 0,04 &euro;</td>
            <td class="num">&asymp; 0,74 &euro;</td>
            <td>no, e l&rsquo;app te lo dice</td>
          </tr>
          <tr>
            <td><b>Google</b> &mdash; Imagen 4 &middot; Veo 3</td>
            <td class="num">&asymp; 0,04 &euro;</td>
            <td class="num">&asymp; 2,94 &euro;</td>
            <td>no, e l&rsquo;app te lo dice</td>
          </tr>
        </tbody>
      </table>
    </div>
    <p class="microcopy">
      Listino verificato a settembre 2026 e scritto in un file di testo modificabile, non nel codice:
      quando un provider cambia prezzo, correggi una riga.
    </p>
  </div>
</section>

<div class="rule"></div>

<!-- ========================================================= COME FUNZIONA -->
<section id="come">
  <div class="wrap">
    <div class="eyebrow">Come funziona</div>
    <h2>Dall&rsquo;idea al risultato<br>in tre passaggi.</h2>

    <div class="grid-3">
      <div class="card reveal">
        <div class="ic"><svg viewBox="0 0 24 24"><path d="M4 5h16M4 12h16M4 19h10"/></svg></div>
        <h3>01 &nbsp;Scegli cosa creare</h3>
        <p>Immagine, modifica di un&rsquo;immagine, video da testo o video da una foto.
        Il resto della finestra si riordina di conseguenza.</p>
      </div>
      <div class="card reveal">
        <div class="ic"><svg viewBox="0 0 24 24"><path d="M12 3l2.4 5.6 6.1.5-4.6 4 1.4 6-5.3-3.2L6.7 19l1.4-6-4.6-4 6.1-.5z"/></svg></div>
        <h3>02 &nbsp;Scegli un preset</h3>
        <p>Ognuno porta con s&eacute; il modello giusto, il workflow e i parametri gi&agrave;
        fissati. Se il modello non c&rsquo;&egrave;, il pulsante te lo dice con peso e licenza.</p>
      </div>
      <div class="card reveal">
        <div class="ic"><svg viewBox="0 0 24 24"><path d="M5 12l4 4L19 6"/><path d="M3 20h18"/></svg></div>
        <h3>03 &nbsp;Scrivi e genera</h3>
        <p>Scrivi in italiano. Sei menu traducono inquadratura, luce e atmosfera nel
        dialetto che quel modello capisce, e ti mostrano cosa aggiungono al prompt.</p>
      </div>
    </div>
  </div>
</section>

<div class="rule"></div>

<!-- ============================================================== PRESET -->
<section id="preset">
  <div class="wrap">
    <div class="eyebrow">Quattordici preset</div>
    <h2>Ognuno sa quale modello<br>gli serve davvero.</h2>
    <p class="lede">
      Step, CFG e sampler vengono dal template ufficiale di ComfyUI per quel
      checkpoint, non da una stima. Sono <b style="color:var(--text)">bloccati</b>: i cursori si
      spengono e dicono perch&eacute;. Restano tuoi prompt, risoluzione, seed e varianti.
    </p>

    <div class="presets">
      <div class="preset reveal"><div class="pn">Iper realistico</div><div class="pm">Krea 2 Turbo</div><div class="pp">8 step &middot; CFG 1 &middot; euler</div></div>
      <div class="preset reveal"><div class="pn">Realismo Krea</div><div class="pm">Krea 2 Turbo</div><div class="pp">8 step &middot; CFG 1 &middot; euler</div></div>
      <div class="preset reveal"><div class="pn">Stesso personaggio</div><div class="pm">FLUX.2 Klein 4B &middot; da una foto</div><div class="pp">20 step &middot; CFG 5 &middot; euler</div></div>
      <div class="preset reveal"><div class="pn">Foto da studio</div><div class="pm">Krea 2 Turbo</div><div class="pp">8 step &middot; CFG 1 &middot; euler</div></div>
      <div class="preset reveal"><div class="pn">Foto cinematografica</div><div class="pm">Krea 2 Turbo</div><div class="pp">8 step &middot; CFG 1 &middot; euler</div></div>
      <div class="preset reveal"><div class="pn">Testo e packaging</div><div class="pm">Qwen-Image 20B</div><div class="pp">8 step &middot; CFG 1 &middot; simple</div></div>
      <div class="preset reveal"><div class="pn">Modifica guidata</div><div class="pm">FLUX.2 Klein 4B &middot; da una foto</div><div class="pp">20 step &middot; CFG 5 &middot; euler</div></div>
      <div class="preset reveal"><div class="pn">Stile anime</div><div class="pm">Illustrious XL</div><div class="pp">28 step &middot; CFG 6 &middot; euler_a</div></div>
      <div class="preset reveal"><div class="pn">Stile manhwa</div><div class="pm">Illustrious XL</div><div class="pp">28 step &middot; CFG 6 &middot; euler_a</div></div>
      <div class="preset reveal"><div class="pn">Anime descrittivo</div><div class="pm">NetaYume Lumina 3.5</div><div class="pp">30 step &middot; CFG 4 &middot; res_multistep</div></div>
      <div class="preset reveal"><div class="pn">Anime NSFW</div><div class="pm">Illustrious XL</div><div class="pp">30 step &middot; CFG 6 &middot; euler_a</div></div>
      <div class="preset reveal"><div class="pn">Realistico NSFW</div><div class="pm">RealVisXL V5</div><div class="pp">30 step &middot; CFG 5 &middot; dpmpp_2m</div></div>
      <div class="preset reveal"><div class="pn">Clip cinematografica</div><div class="pm">Wan 2.2 TI2V 5B &middot; video</div><div class="pp">20 step &middot; CFG 5 &middot; 1280&times;720</div></div>
      <div class="preset reveal"><div class="pn">Foto in movimento</div><div class="pm">Wan 2.2 TI2V 5B &middot; video</div><div class="pp">20 step &middot; CFG 5 &middot; 1280&times;720</div></div>
    </div>
  </div>
</section>

<div class="rule"></div>

<!-- ============================================================ GALLERIA -->
<section id="galleria">
  <div class="wrap">
    <div class="eyebrow">Fatto con EasyFrame</div>
    <h2>Sei preset, sei risultati.</h2>
    <p class="lede">
      Sotto ogni immagine c&rsquo;&egrave; il preset che l&rsquo;ha prodotta e i parametri esatti:
      puoi rifarla identica, o cambiarne uno solo e vedere cosa succede.
    </p>

    <div class="gallery">
      <div class="tile reveal"><div class="art"><img src="/easyframe/galleria-iper-realistico.jpg" alt="Ritratto fotografico di un uomo brizzolato con cappotto nero accanto a una finestra" loading="lazy"></div>
        <div class="meta"><div class="pname">Iper realistico</div>
        <div class="ptext">&ldquo;portrait of a grey-haired man in a black wool coat standing by a tall window&rdquo;</div>
        <div class="pset">Krea 2 Turbo &middot; 8 step &middot; CFG 1 &middot; 1216&times;832 &middot; seed 2701</div></div></div>

      <div class="tile reveal"><div class="art"><img src="/easyframe/galleria-foto-cinematografica.jpg" alt="Un ciclista attraversa di notte una strada bagnata dalla pioggia, con le luci dei negozi riflesse sull'asfalto" loading="lazy"></div>
        <div class="meta"><div class="pname">Foto cinematografica</div>
        <div class="ptext">&ldquo;a lone cyclist crossing a rainy city street at night, neon shop lights reflected on wet asphalt&rdquo;</div>
        <div class="pset">Krea 2 Turbo &middot; 8 step &middot; CFG 1 &middot; 1344&times;768 &middot; seed 3107</div></div></div>

      <div class="tile reveal"><div class="art"><img src="/easyframe/galleria-stile-anime.jpg" alt="Illustrazione anime di una ragazza in uniforme scolastica con i capelli mossi dal vento al tramonto" loading="lazy"></div>
        <div class="meta"><div class="pname">Stile anime</div>
        <div class="ptext">&ldquo;1girl, school uniform, rooftop, sunset, wind, looking at viewer, upper body&rdquo;</div>
        <div class="pset">Illustrious XL &middot; 28 step &middot; CFG 6 &middot; 832&times;1216 &middot; seed 4409</div></div></div>

      <div class="tile reveal"><div class="art"><img src="/easyframe/galleria-foto-da-studio.jpg" alt="Flacone di profumo in vetro con liquido ambrato su un piedistallo di pietra grigia" loading="lazy"></div>
        <div class="meta"><div class="pname">Foto da studio</div>
        <div class="ptext">&ldquo;a glass perfume bottle with amber liquid on a rough grey stone pedestal&rdquo;</div>
        <div class="pset">Krea 2 Turbo &middot; 8 step &middot; CFG 1 &middot; 1024&times;1024 &middot; seed 5501</div></div></div>

      <div class="tile reveal"><div class="art"><img src="/easyframe/galleria-anime-descrittivo.jpg" alt="Illustrazione di una ragazza dai lunghi capelli argento che legge un grande libro in una biblioteca antica" loading="lazy"></div>
        <div class="meta"><div class="pname">Anime descrittivo</div>
        <div class="ptext">&ldquo;a girl with long silver hair reading a large book in a sunlit old library, dust floating in the light&rdquo;</div>
        <div class="pset">NetaYume Lumina 3.5 &middot; 30 step &middot; CFG 4 &middot; 1024&times;1024 &middot; seed 6203</div></div></div>

      <div class="tile reveal"><div class="art"><img src="/easyframe/galleria-realismo-krea.jpg" alt="Sei amici ridono attorno a un tavolo di legno all'aperto, sotto una fila di lampadine" loading="lazy"></div>
        <div class="meta"><div class="pname">Realismo Krea</div>
        <div class="ptext">&ldquo;six friends laughing around a wooden table at an outdoor cafe in the evening, string lights overhead&rdquo;</div>
        <div class="pset">Krea 2 Turbo &middot; 8 step &middot; CFG 1 &middot; 1024&times;1024 &middot; seed 7019</div></div></div>
    </div>
  </div>
</section>

<div class="rule"></div>

<!-- ============================================================ MODELLI -->
<section id="modelli">
  <div class="wrap">
    <div class="eyebrow">Modelli garantiti</div>
    <h2>L&rsquo;app non si adatta ai<br>modelli che hai. Porta i suoi.</h2>
    <p class="lede">
      &Egrave; la differenza fra un preset che funziona sul tuo computer e uno che funziona
      su qualunque computer. Dieci corredi, ognuno con la sua licenza scritta accanto:
      nove sono usabili commercialmente senza condizioni, e per quello che ne pone
      (Krea 2: sotto il milione di ricavi, con obbligo di attribuzione) l&rsquo;app te le
      fa leggere e accettare prima di scaricare.
    </p>
    <p class="lede">
      Accanto trovi i <b style="color:var(--text)">corredi della community</b>, tenuti
      separati apposta: sono modelli di terze parti, con licenze che non sono le nostre.
      L&rsquo;app scrive i loro limiti sul pulsante &mdash; se puoi vendere le immagini, se
      l&rsquo;autore va citato, se il catalogo chiede un token &mdash; prima che tu scarichi.
    </p>

    <div class="split">
      <div class="figure reveal">
        <img src="/easyframe/app-models-card.png" alt="Il pannello dei modelli di EasyFrame: per ogni corredo nome, peso, licenza, a cosa serve e se e' installato">
      </div>
      <div>
        <ol class="rules reveal">
          <li><b>Parte solo quando lo chiedi</b>, e la dimensione &egrave; scritta sul pulsante prima di cliccarlo.</li>
          <li><b>Si annulla senza perdere niente</b>: alla ripresa riparte dal byte dove si era fermato.</li>
          <li><b>SHA-256 verificato</b> prima della consegna, poi rinomina atomica: ComfyUI non vede mai un modello a met&agrave;.</li>
          <li><b>Non sovrascrive mai i tuoi file</b>, nemmeno una copia diversa con lo stesso nome.</li>
          <li><b>Lo spazio si controlla prima</b>, non a 8 GB su 9.</li>
        </ol>
      </div>
    </div>

    <div class="tablewrap reveal">
      <table>
        <thead>
          <tr><th>Corredo</th><th></th><th>Peso</th><th>VRAM</th><th>Licenza</th></tr>
        </thead>
        <tbody>
          <tr><td><b>FLUX.2 Klein 4B</b></td><td><span class="tag serie">di serie</span></td><td class="num">11,6 GB</td><td class="num">~9 GB</td><td>Apache 2.0</td></tr>
          <tr><td><b>Wan 2.2 TI2V 5B</b></td><td><span class="tag serie">di serie</span></td><td class="num">16,9 GB</td><td class="num">~22 GB</td><td>Apache 2.0</td></tr>
          <tr><td><b>Illustrious XL</b></td><td><span class="tag opt">opzionale</span></td><td class="num">6,5 GB</td><td class="num">~8 GB</td><td>OpenRAIL++-M</td></tr>
          <tr><td><b>NetaYume Lumina 3.5</b></td><td><span class="tag opt">opzionale</span></td><td class="num">9,9 GB</td><td class="num">~11,5 GB</td><td>Apache 2.0</td></tr>
          <tr><td><b>RealVisXL V5</b></td><td><span class="tag opt">opzionale</span></td><td class="num">6,5 GB</td><td class="num">~8 GB</td><td>OpenRAIL++-M</td></tr>
          <tr><td><b>Krea 2 Turbo</b></td><td><span class="tag opt">opzionale</span></td><td class="num">17,4 GB</td><td class="num">~11,5 GB</td><td>Krea 2 Community</td></tr>
          <tr><td><b>Qwen-Image 20B</b></td><td><span class="tag opt">opzionale</span></td><td class="num">29,6 GB</td><td class="num">~24 GB</td><td>Apache 2.0</td></tr>
          <tr><td><b>SeedVR2 3B (restauro)</b></td><td><span class="tag opt">opzionale</span></td><td class="num">3,7 GB</td><td class="num">~8,5 GB</td><td>Apache 2.0</td></tr>
          <tr><td><b>Qwen3-VL 4B (prompt da immagine)</b></td><td><span class="tag opt">opzionale</span></td><td class="num">4,9 GB</td><td class="num">~9 GB</td><td>Apache 2.0</td></tr>
          <tr><td><b>Ingranditori 4&times;</b></td><td><span class="tag opt">opzionale</span></td><td class="num">56,5 MB</td><td class="num">~2 GB</td><td>CC BY 4.0</td></tr>
        </tbody>
      </table>
    </div>

    <div class="quote reveal">
      <p class="big">~24 GB VRAM su 12, <em>oltre la tua scheda</em></p>
      <p class="sub">
        Ogni corredo dichiara la VRAM che chiede e l&rsquo;app la confronta con quella che
        misura sulla tua macchina. Due numeri veri, non un bollino
        &ldquo;ottimo per la tua GPU&rdquo;. E quando &egrave; EasyFrame ad avviare ComfyUI,
        sceglie gli argomenti di memoria in base alla scheda che ha trovato.
      </p>
    </div>
  </div>
</section>

<div class="rule"></div>

<!-- ============================================================= COMFYUI -->
<section id="comfy">
  <div class="wrap">
    <div class="grid-2" style="margin-top:0">
      <div class="card reveal">
        <div class="ic"><svg viewBox="0 0 24 24"><path d="M12 3v12"/><path d="M7 10l5 5 5-5"/><path d="M4 21h16"/></svg></div>
        <h3>Non hai ComfyUI? Lo installa lui.</h3>
        <p>Scarica il pacchetto portable ufficiale di Comfy-Org (2,0 GB, verificato con
        il suo SHA-256) e lo estrae dove scegli tu. Non tocca il Python di sistema e si
        disinstalla cancellando la cartella.</p>
      </div>
      <div class="card reveal">
        <div class="ic"><svg viewBox="0 0 24 24"><circle cx="11" cy="11" r="7"/><path d="M20 20l-3.6-3.6"/></svg></div>
        <h3>Ce l&rsquo;hai gi&agrave;? Lo trova da solo.</h3>
        <p>Anche con decine di GB di modelli gi&agrave; scaricati: ne legge le cartelle
        condivise, riconosce quello che possiedi e non ti fa scaricare niente due volte.</p>
      </div>
    </div>
  </div>
</section>

<div class="rule"></div>

<!-- ============================================================== AGENTI -->
<section id="agenti">
  <div class="wrap">
    <div class="eyebrow">Per gli agenti</div>
    <h2>Claude genera da solo.<br>Con i limiti che gli dai tu.</h2>
    <p class="lede">
      EasyFrame espone sei strumenti via <b style="color:var(--text)">MCP</b>: stato, preset,
      costo, modelli, genera e prompt da immagine. Un agente puo' chiedere quanto costa prima di generare, sapere
      quale modello manca, e produrre il file. Sotto c&rsquo;&egrave; lo stesso motore della finestra,
      quindi non puo' fare niente che l&rsquo;app non farebbe.
    </p>

    <pre class="code"><span class="c">// una riga di terminale scrive la configurazione per te</span>
<span class="k">python</span> main.py --mcp-config

<span class="c">// e quello che esce si incolla nel client</span>
&#123;
  <span class="s">"mcpServers"</span>: &#123;
    <span class="s">"easyframe"</span>: &#123;
      <span class="s">"command"</span>: <span class="s">"...\EasyFrame-mcp.exe"</span>,
      <span class="s">"args"</span>: [<span class="s">"--mcp"</span>]
    &#125;
  &#125;
&#125;</pre>

    <div class="guards">
      <div class="guard reveal">
        <span class="no">non scarica</span>
        <b>Nessun download, mai</b>
        <p>Un agente puo' sapere che mancano 29,6 GB, non farli partire. Scaricare resta
        un gesto tuo, dentro l&rsquo;applicazione, dopo aver visto il peso.</p>
      </div>
      <div class="guard reveal">
        <span class="no">non spende</span>
        <b>Il cloud parte spento</b>
        <p>Si accende con <span class="mono">--consenti-cloud</span> e un tetto in euro.
        Superato quello, la generazione si rifiuta prima di chiamare il provider.</p>
      </div>
      <div class="guard reveal">
        <span class="no">non configura</span>
        <b>Non tocca le impostazioni</b>
        <p>Legge la tua configurazione e basta. Le chiavi API, le cartelle e i preset
        restano come li hai lasciati.</p>
      </div>
    </div>
  </div>
</section>

<div class="rule"></div>

<!-- ============================================================== LIMITI -->
<section id="limiti">
  <div class="wrap">
    <div class="eyebrow" style="color:var(--warn)">Quello che non fa</div>
    <h2>I limiti, scritti prima<br>che tu paghi.</h2>
    <p class="lede">
      Nessuno strumento di generazione fa tutto bene. Questi sono i punti dove EasyFrame
      &egrave; debole, e preferisco che tu li sappia adesso.
    </p>

    <details class="limits-box">
      <summary class="limits-toggle">
        <span class="apri">Mostra i nove limiti</span>
        <span class="chiudi">Nascondi i limiti</span>
      </summary>
      <div class="limits">
      <div class="limit">
        <b>Il testo dentro le immagini</b>
        <p>Lo scrive bene solo se glielo chiedi esplicitamente, e al meglio con il
        corredo Qwen-Image, che vuole ~24 GB di VRAM. Sotto quella soglia conviene il cloud.</p>
      </div>
      <div class="limit">
        <b>Le scene con molte persone, sul modello di serie</b>
        <p>FLUX.2 Klein tiene fino a tre figure; da quattro in su, a 1216&times;832, volti e
        mani si sfaldano &mdash; e non sono gli step, sono i pixel. L&rsquo;app se ne accorge dal
        prompt e propone il formato pi&ugrave; grande che entra nella tua scheda. Col corredo
        Krea 2 il problema non si pone: sei figure restano corrette gi&agrave; a 1024&times;1024,
        misurato, e l&rsquo;avviso infatti non compare.</p>
      </div>
      <div class="limit">
        <b>Le geometrie meccaniche complesse</b>
        <p>Biciclette, ingranaggi, strutture articolate restano il punto debole di un
        modello da 4 miliardi di parametri.</p>
      </div>
      <div class="limit">
        <b>I preset anime vogliono tag inglesi</b>
        <p>L&rsquo;app te lo dice mentre scrivi e i menu li generano al posto tuo, ma il
        modello resta quello: in italiano capisce molto meno.</p>
      </div>
      <div class="limit">
        <b>Il video 720p da cinque secondi</b>
        <p>Chiede pi&ugrave; di 12 GB di VRAM. L&rsquo;avviso non blocca mai e propone durata e
        risoluzione che rientrano davvero nella tua scheda.</p>
      </div>
      <div class="limit">
        <b>Le ricariche a token sono simulate</b>
        <p>In questa versione il saldo si accredita senza alcun pagamento reale. La
        modalit&agrave; con la tua chiave API &egrave; invece completa e funzionante.</p>
      </div>
      <div class="limit">
        <b>I preset per adulti</b>
        <p>Generano soltanto soggetti adulti, con una barriera esplicita nel prompt
        negativo. Resta una barriera probabilistica, non un filtro.</p>
      </div>
      <div class="limit">
        <b>Windows avvisa al primo avvio</b>
        <p>L&rsquo;installer non ha ancora una firma digitale, quindi SmartScreen mostra
        &ldquo;Windows ha protetto il PC&rdquo;. Si prosegue con Ulteriori informazioni
        &rarr; Esegui comunque.</p>
      </div>
      <div class="limit">
        <b>macOS &egrave; in beta</b>
        <p>Il pacchetto per Mac con Apple Silicon passa i test automatici su macOS, ma nessuno l&rsquo;ha
        ancora usato con ComfyUI su un Mac vero: su Metal alcuni modelli fp8 potrebbero non caricarsi.
        Non ha la firma Apple, quindi la prima volta si apre da Impostazioni di Sistema &rarr; Privacy e
        sicurezza &rarr; Apri comunque, e ComfyUI va installato a parte con ComfyUI Desktop.</p>
      </div>
      </div>
    </details>
  </div>
</section>

<div class="rule"></div>

<!-- ============================================================== PREZZO -->
<section id="prezzo">
  <div class="wrap" style="text-align:center">
    <div class="eyebrow" style="justify-content:center">Prezzo</div>
    <h2>Si paga una volta<br>e resta tua.</h2>

    <div class="price-wrap reveal">
      <div class="price">
        <div class="kicker">Acquisto unico</div>
        <div class="amount">
          <span class="was strike">45&nbsp;&euro;</span>
          <span class="now">29&nbsp;&euro;</span>
        </div>
        <span class="off">Prezzo di lancio &minus;35%</span>
        <p class="once">Nessun abbonamento, nessun credito che scade, nessun tetto di generazioni.</p>

        <ul class="plist">
          <li><svg viewBox="0 0 24 24"><path d="M5 12l4 4L19 6"/></svg>L&rsquo;applicazione per Windows 10 e 11, pi&ugrave; la beta per Mac con Apple Silicon</li>
          <li><svg viewBox="0 0 24 24"><path d="M5 12l4 4L19 6"/></svg>Quattordici preset e dodici workflow gi&agrave; pronti</li>
          <li><svg viewBox="0 0 24 24"><path d="M5 12l4 4L19 6"/></svg>Il prompt da un&rsquo;immagine: la legge e lo scrive per il preset</li>
          <li><svg viewBox="0 0 24 24"><path d="M5 12l4 4L19 6"/></svg>Il download verificato di tutti e dieci i corredi di modelli</li>
          <li><svg viewBox="0 0 24 24"><path d="M5 12l4 4L19 6"/></svg>L&rsquo;installazione automatica di ComfyUI, se non ce l&rsquo;hai</li>
          <li><svg viewBox="0 0 24 24"><path d="M5 12l4 4L19 6"/></svg>Generazione cloud con la tua chiave API, senza ricarichi</li>
          <li><svg viewBox="0 0 24 24"><path d="M5 12l4 4L19 6"/></svg>Aggiornamenti gratuiti a vita</li>
          <li><svg viewBox="0 0 24 24"><path d="M5 12l4 4L19 6"/></svg>Rimborso completo entro 7 giorni</li>
        </ul>

        <a class="btn btn-primary" href="https://gianlucaiscool5.gumroad.com/l/easyframe">Comprala &mdash; 29&nbsp;&euro;</a>
        <p class="safe">Pagamento sicuro via Gumroad &middot; download immediato</p>
      </div>
    </div>
  </div>
</section>

<div class="rule"></div>

<!-- ================================================================= FAQ -->
<section id="faq">
  <div class="wrap">
    <div class="eyebrow">Domande</div>
    <h2>Buono a sapersi.</h2>

    <div class="faq">
      <details>
        <summary>Devo saper usare ComfyUI?</summary>
        <div class="ans">No. Se ce l&rsquo;hai, EasyFrame lo trova e ne usa le cartelle; se non ce l&rsquo;hai,
        te lo installa. In nessuno dei due casi devi aprire un grafo a nodi: i workflow sono
        gi&agrave; scritti e collegati ai preset.</div>
      </details>
      <details>
        <summary>Che scheda video mi serve?</summary>
        <div class="ans">Per le immagini in locale, una GPU NVIDIA da <b>10 GB</b> o pi&ugrave; e circa 12 GB
        di disco. Per i video nel formato pieno servono <b>16 GB</b> o pi&ugrave; e circa 17 GB di disco.
        Per il solo cloud basta qualunque PC con una connessione e una chiave API.</div>
      </details>
      <details>
        <summary>La generazione in locale &egrave; davvero gratis?</summary>
        <div class="ans">S&igrave;. Gira sulla tua GPU, non passa da nessun server e non c&rsquo;&egrave; alcun
        costo per immagine: paghi solo la corrente. Non c&rsquo;&egrave; un tetto di generazioni.</div>
      </details>
      <details>
        <summary>Quanto costa il cloud?</summary>
        <div class="ans">Paghi il provider al suo prezzo, con la tua chiave: circa <b>0,04 &euro;</b> a
        immagine, <b>0,74 &euro;</b> per otto secondi di video su OpenAI e <b>2,94 &euro;</b> su Google.
        EasyFrame non applica alcun ricarico e ti mostra la stima prima che tu clicchi.</div>
      </details>
      <details>
        <summary>Devo scaricare tutti i modelli subito?</summary>
        <div class="ans">No. Per iniziare servono i due corredi di serie, e li scarichi quando
        vuoi. Gli altri otto si scaricano uno per volta, dal preset che li richiede o dal
        pannello dei modelli. Il download parte sempre e solo quando lo chiedi tu.</div>
      </details>
      <details>
        <summary>Posso usare le immagini per lavoro?</summary>
        <div class="ans">Nove corredi su dieci hanno licenze utilizzabili anche commercialmente
        (Apache 2.0, OpenRAIL++-M, CC BY 4.0); Krea 2 lo consente sotto il milione di dollari
        di ricavi annui, con attribuzione. Le condizioni esatte restano quelle di ciascuna
        licenza, che l&rsquo;app ti mostra accanto al modello prima di scaricarlo.</div>
      </details>
      <details>
        <summary>Su quali sistemi operativi gira?</summary>
        <div class="ans">Windows 10 e 11. Per i Mac con Apple Silicon (M1 e successivi) c&rsquo;&egrave; una
        <b>beta</b>: compilata e verificata automaticamente su macOS, non ancora provata su un Mac vero
        con ComfyUI, e senza firma Apple. Su Mac ComfyUI si installa a parte con ComfyUI Desktop. Per
        Linux non c&rsquo;&egrave; un pacchetto.</div>
      </details>
      <details>
        <summary>Come funzionano i rimborsi?</summary>
        <div class="ans">Hai 7 giorni dall&rsquo;acquisto: se EasyFrame non fa per te, chiedi il
        rimborso da Gumroad e ricevi l&rsquo;intero importo.</div>
      </details>
    </div>
  </div>
</section>

<!-- ============================================================== CHIUSA -->
<section class="final">
  <div class="wrap">
    <h2>Smetti di cliccare<br>e sperare.</h2>
    <p class="lede" style="margin:24px auto 0; text-align:center">
      Installi una volta, resta tua per sempre, e in locale non paghi pi&ugrave; niente.
    </p>
    <div class="cta-row" style="justify-content:center">
      <a class="btn btn-primary" href="https://gianlucaiscool5.gumroad.com/l/easyframe">
        Comprala &mdash; <span class="strike">45&nbsp;&euro;</span> 29&nbsp;&euro;
      </a>
    </div>
    <p class="microcopy" style="text-align:center">
      Acquisto unico &middot; nessun abbonamento &middot; pagamento via Gumroad
    </p>
  </div>
</section>

<footer>
  <div class="wrap foot-row">
    <div class="brand" style="font-size:15px"><span class="mark"></span>EasyFrame&nbsp;<span style="color:var(--faint); font-weight:400">0.1.1</span></div>
    <div>1&nbsp;520 verifiche automatiche su ogni versione &middot; listino cloud verificato a settembre 2026</div>
  </div>
</footer>
<script is:inline>
(function(){
  document.documentElement.classList.add('js');

  // I limiti si aprono a cascata da un solo pulsante. Senza JS la classe non
  // arriva mai e restano semplicemente visibili all'apertura del blocco.
  var limiti = document.querySelector('.limits-box');
  if (limiti) {
    limiti.addEventListener('toggle', function(){
      if (limiti.open) {
        requestAnimationFrame(function(){ limiti.classList.add('ready'); });
      } else {
        limiti.classList.remove('ready');
      }
    });
  }

  var nav = document.getElementById('nav');
  var onScroll = function(){ nav.classList.toggle('scrolled', window.scrollY > 20); };
  onScroll();
  window.addEventListener('scroll', onScroll, {passive:true});

  var reduce = window.matchMedia('(prefers-reduced-motion: reduce)').matches;
  var items = document.querySelectorAll('.reveal');
  if (reduce || !('IntersectionObserver' in window)) {
    items.forEach(function(el){ el.classList.add('in'); });
  } else {
    var io = new IntersectionObserver(function(entries){
      entries.forEach(function(e){
        if (e.isIntersecting){ e.target.classList.add('in'); io.unobserve(e.target); }
      });
    }, {rootMargin:'0px 0px -8% 0px', threshold:0.08});
    items.forEach(function(el, i){
      el.style.transitionDelay = (Math.min(i % 4, 3) * 70) + 'ms';
      io.observe(el);
    });
  }

  // Riflesso che segue il puntatore dentro le card: costa poco e le rende vive.
  if (!reduce && window.matchMedia('(hover: hover)').matches) {
    document.querySelectorAll('.card').forEach(function(card){
      card.addEventListener('pointermove', function(ev){
        var r = card.getBoundingClientRect();
        card.style.setProperty('--mx', (ev.clientX - r.left) + 'px');
        card.style.setProperty('--my', (ev.clientY - r.top) + 'px');
      });
    });
  }
})();
</script>
</body>
</html>
